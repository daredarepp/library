<!DOCTYPE html>
<html>
  <head>
    <% include presets/head %>
  </head>
  <body>
    <% <!-- Admin index --> %>
    <% if (typeof reset === 'undefined') { %>

      <% include presets/admin_presets/index %>

    <% <!-- Admin reset --> %>
    <% } else { %>
      
      <% include presets/admin_presets/populate %>
      
    <% } %>
  </body>
</html>
